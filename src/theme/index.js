import {createTheme} from '@rneui/themed';

export default REtheme = createTheme({
  lightColors: {
    primary: '#0d0d0d',
  },
  darkColors: {
    primary: 'blue',
  },
  components: {
    Button: {
      raised: true,
    },
  },
});
